export interface User {
  id: number;
  name: string;
  email: string;
  password: string;
  role: string; // Add the role property here
  // Add more properties as needed (e.g., profile picture, role, etc.)
}
